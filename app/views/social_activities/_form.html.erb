<%= form_for(@social_activity) do |f| %>
  <% if @social_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@social_activity.errors.count, "error") %> prohibited this social_activity from being saved:</h2>

      <ul>
      <% @social_activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
